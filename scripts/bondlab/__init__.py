"""Одноразовый трек: выгрузка с MOEX ISS и сверка доходностей.

Код здесь **одноразовый** (`docs/PARALLEL-TRACK.md`, «Режим работы»). Он будет
выброшен, когда дойдёт дело до боевого `adapters/moex.py`. Переживут
переписывание три вещи, и внимание тратится на них:

* сохранённые ответы ISS в `tests/fixtures/iss/`;
* тесты с эталонными числами;
* записи в `ASSUMPTIONS.md`.

Хранилище отдельное — `data/bondlab/`, SQLite. Нулевое пересечение с боевой
базой: красная линия трека.
"""
