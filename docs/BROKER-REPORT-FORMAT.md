# Формат отчёта брокера: что показала разведка

Разбор **15 обезличенных отчётов за 2017–2020 годы** («ОТЧЕТ ПО СДЕЛКАМ И
ОПЕРАЦИЯМ С ЦЕННЫМИ БУМАГАМИ И ВАЛЮТНЫМИ ИНСТРУМЕНТАМИ…»). Это шаг 0 этапа 1:
форма документа, словарь операций, формы чисел и то, как всё это менялось за три
года.

Инструменты разбора: `tools/report_structure.py dump` и `compare`. Числа в
таблицах ниже — «в скольких отчётах из 14 встретилось» (пятнадцатый разбирался
отдельно и вошёл в выводы).

### Вторая разведка: необезличенный архив 2024–2026

Первая разведка шла по обезличенным отчётам 2017–2020. Вторая — по выжимке
(`report_structure.py digest`) с **14 отчётов 2024–2026 годов**, снятой владельцем
архива на исходных файлах. Выжимка содержит только форму: подписи разделов,
заголовки колонок, словарь типов операций, формы чисел и дат; сумм, имён и
реквизитов в ней нет.

Что она показала:

* **форма не изменилась за девять лет**: тот же набор разделов, те же колонки,
  та же нумерация. Разбор, написанный по архиву 2017–2020, подошёл отчётам 2026
  года без правок;
* кодировки: 12 файлов UTF-8 с BOM, 2 — windows-1251. Объявление и содержимое
  совпадают;
* ширина сетки 27 или 29 колонок — разные годы, один формат;
* новых типов операций нет. Неопознанными заголовками остались ровно три:
  «Плановая», «Фактическая» (второй ярус шапки — это не колонка, а уточнение) и
  «по цене закрытия» (подпись в разделе оценки активов);
* подтвердились формы чисел с шестью знаками после точки и отрицательные
  количества.

---

## 1. Это выгрузка из Excel, а не веб-страница

```
<meta name="ProgId" content="Excel.Sheet">
<meta name="Generator" content="Microsoft Excel 15">
```

Следствия, каждое из которых важно парсеру:

| Наблюдение | Что из этого следует |
|---|---|
| **Весь отчёт — одна таблица** `<table>`, 115 строк × 21 колонка | `find_sections` в `mapping_v1` ищет секции как **отдельные таблицы** и на этом отчёте находит одну безымянную. Секции здесь — строки внутри единственной таблицы |
| 270 `colspan` и 51 `rowspan` | без разворачивания объединений данные съезжают относительно шапки: «Место совершения сделки» из шапки стоит над «Дата поставки фактическая» в данных |
| Шапка секции сделок — **две строки**: основная плюс «Плановая / Фактическая» под объединёнными «Дата оплаты» и «Дата поставки» | одна логическая колонка занимает две физические; `resolve_columns` рассчитан на одну строку заголовков |
| Числовые ячейки несут атрибут `x:num` | в необезличенном отчёте там лежит **точное значение** отдельно от видимого текста. Для парсера — подарок (надёжный признак числа), для обезличивания — главная ловушка |
| `<style>` с правилами Excel внутри `<!-- -->` | `tables.py` затаскивает текст `<style>` в `preceding_text` первой таблицы: мусор в якорях |
| Два `<img>` | в боевом отчёте это логотип и, возможно, факсимиле подписи |
| Кодировка UTF-8 с BOM, переводы строк CRLF | `decode_report` справляется; BOM снимается |
| В архиве встречаются файлы в UTF-8 с метой `charset=windows-1251` | объявлению верить нельзя: CP1251 декодирует такой файл без ошибок и молча превращает текст в `РЎРѕСЃС‚РѕСЏРЅРёРµ`. Кодировка выбирается по качеству результата |
| Объявление кодировки бывает в двух формах: `<meta http-equiv="Content-Type">` и HTML5 `<meta charset>` | при выводе через libxml2 первая форма исчезает. Любой инструмент, переписывающий отчёт, обязан оставить объявление в HTML5-форме, иначе файл открывается мусором в браузере при исправных байтах |

## 2. Разделительные числовые форматы

| Что | Как выглядит | Замечание |
|---|---|---|
| Разделитель разрядов | обычный пробел (`1 234.56`), **не** `\xa0` | `parse_decimal` справляется, но фикстура должна содержать и этот вариант |
| Десятичный разделитель | **точка**, не запятая | то есть «русские числа» у этого брокера — пробел плюс точка |
| Знаков после точки | 0, 2, 3, 4 и **6** | по всему архиву: `0 000.00` (5601 раз), `00` (2117), `0 000.0000` (263), `0 000.000` (27), `0 000.000000` (10). Шесть знаков — курс валютной пары; округление до копейки потеряет цену |
| Отрицательные количества | `-00` (3 раза за архив) | количество бывает со знаком минус: короткая позиция или возврат займа |
| Отрицательные | `-1 234.56` | знак перед числом |
| Даты | `00.00.0000` (`дд.мм.гггг`) | время отдельной колонкой, `чч:мм:сс` |
| Номер сделки | `B-000000-000000` | буква, дефис, две группы цифр |
| Валюта | `RUR`, не `RUB` | в `formats._CURRENCY_ALIASES` уже есть |

## 3. Секции

Нумерация разрежена (1, 1.1, 2, 4, 5, 5.1, 5.4, 5.9, 5.10, 8, 8.1, 8.1.1, 8.2):
брокер печатает только непустые секции, **номера не подряд**, а вложенность
доходит до **трёх ступеней** (`8.1.1`). Значит опознавать секции нужно по тексту
подписи, а не по номеру и не по порядку.

### Что встречается в архиве

| Секция | В скольких отчётах | Замечание |
|---|---|---|
| `1. Состояние денежных средств на счете` | 14/14 | |
| `1.1 Биржевые площадки и внебиржевой рынок` | 4/14 | только до 2018 года; позже разложение остатка идёт плоским списком внутри секции 1 |
| `2. Состояние портфеля ценных бумаг` | 14/14 | |
| `4. Оценка активов` | 14/14 | цены, этап 4 |
| `5. Сделки с ценными бумагами` | 4/14 | старая формулировка |
| `5. Сделки с ценными бумагами и валютными инструментами` | 10/14 | с 2018 года |
| `5.1 Биржевые сделки с ценными бумагами` | 14/14 | |
| `5.4 Сделки займа ценных бумаг` | 8/14 | |
| `5.9 Незавершенные сделки` | 1/14 | сделка, не рассчитанная на дату отчёта |
| `5.10 Исполнение обязательств по сделке` | 14/14 | вторая часть РЕПО |
| `8. Неторговые операции` | 14/14 | заголовок верхнего уровня |
| `8.1 Неторговые операции с ДС` | 7/14 | до 2019 года |
| `8.1.1 Зачислено/списано ДС по неторговым операциям` | 7/14 | с 2019 года, **третья ступень нумерации** |
| `8.2 Неторговые операции с ЦБ` | 8/14 | движение бумаг **без денег** |

Две подписи секции 5 и пара 8.1 / 8.1.1 — это одно и то же содержимое под
разными названиями в разные годы. Якорение по подписи обязано знать оба варианта.

| Секция | Содержимое | Нужна этапу 1 |
|---|---|---|
| `1. Состояние денежных средств на счете` | входящий и исходящий остаток, плановые остатки | **да**: контрольная точка для сверки денег |
| `1.1 Биржевые площадки и внебиржевой рынок` | разложение остатка: свободные средства, начисленная комиссия, задолженность; сальдо торговых и неторговых операций; **уплаченная комиссия по восьми видам** | **да**: виды комиссий перечислены явно (брокера, торговой системы, иных депозитариев, депозитария брокера, за необеспеченную позицию по ДС и по ЦБ, за принудительное закрытие, гербовый сбор) |
| `2. Состояние портфеля ценных бумаг` | по бумаге: наименование, эмитент, номер гос. регистрации, ISIN, количество на начало и конец, зачислено, списано, к зачислению, к выводу, плановое количество, цена закрытия, стоимость, доля, НКД, место хранения | **да**: контрольные количества для сверки бумаг |
| `4. Оценка активов` | оценка портфеля и общая стоимость на начало и конец | нет: это цены, этап 4 |
| `5. Сделки с ценными бумагами` → `5.1 Биржевые сделки` | номер сделки, дата, время, вид, цена, валюта цены, количество, НКД, сумма, валюта суммы, брокерская комиссия, валюта комиссии, комиссия ТС, гербовый сбор, даты оплаты и поставки (плановая и фактическая), место совершения | **да**: основной источник событий |
| `5.4 Сделки займа ценных бумаг` | то же плюс «% по сделке» и «Тип сделки займа» (1-я и 2-я часть) | **да**, и это сюрприз: займы бумаг в журнале этапа 1 типа события не имеют |
| `5.10 Исполнение обязательств по сделке` | то же плюс «Тип сделки РЕПО» | **да**, с той же оговоркой |
| `8.1 Неторговые операции с ДС` | дата, тип операции, сумма, валюта, комментарий | **да**: купоны, дивиденды, налоги, ввод и вывод средств |
| подписи, подтверждение клиента, дата формирования | служебное | нет |

Внутри секций сделок строки сгруппированы по выпуску: **строка-подзаголовок**
вида `ООО "Мэйл.Ру Финанс" RU000A103QK3 4B02-01-00566-R-001P RUR` (эмитент, ISIN,
номер гос. регистрации, валюта), затем сделки, затем `Итого по выпуску:` с
подстроками «оборот» и «изменение», затем `Общий итог:`. То есть **инструмент
сделки берётся не из её строки, а из подзаголовка группы** — принципиально иначе,
чем предполагает `mapping_v1`.

## 4. Словарь операций по архиву

Собран командой `report_structure.py compare архив/*.html`. Разбивка по секциям
принципиальна: одно и то же слово в денежной и в бумажной секции означает разные
события журнала.

### Денежные операции (8.1 и 8.1.1)

| Тип операции | Что это в журнале |
|---|---|
| `Ввод ДС` | `CASH_IN` |
| `Вывод ДС` | `CASH_OUT` |
| `Погашение купона` | `COUPON` |
| `Погашение номинала` | **выплата**: `MATURITY` или `AMORTIZATION` (A-20) |
| `Погашение облигации` | **маркер с нулевой суммой**, события не порождает (A-20) |
| `Доход по финансовым инструментам` | `DIVIDEND`; что это дивиденд, видно **только из комментария** |
| `Налог` | `TAX` |

### Операции с бумагами (8.2)

| Тип операции | Что это в журнале |
|---|---|
| `Ввод ЦБ` | перевод бумаг извне: `TRANSFER` без денежного эффекта |
| `Конвертация ЦБ` | `CONVERSION`, две строки одной даты с противоположными знаками. У списываемой стороны **ISIN не напечатан**, а строка названа устаревшим наименованием — бумага восстанавливается из комментария (A-29) |
| `Погашение облигации` | списание бумаги при погашении |

Секция 8.2 — это **вторая сторона** событий: денег в ней нет, есть дата, тип,
бумага и количество. Колонки: `Дата | Тип операции | Наименование ЦБ | Номер гос.
регистрации | ISIN | Эмитент | Количество ЦБ | Комментарий`.

### Виды сделок (секции 5.*)

| Значение | Где встречается |
|---|---|
| `Покупка`, `Продажа` | 5.1, 5.10 |
| `Выдача займа ценных бумаг`, `Возврат займа ценных бумаг` | 5.4, 5.9 |
| `1-я часть`, `2-я часть` | `Тип сделки займа`, `Тип сделки РЕПО`, `Тип незавершенной сделки` |

**Погашение печатается тремя строками** на одну дату (A-20):

```
8.1.1  Погашение облигации   0.00        ← маркер, суммы нет
8.1.1  Погашение номинала    12 345.67   ← выплата, идёт в журнал
8.2    Погашение облигации   100 шт.     ← списание бумаги, идёт в журнал
```

Задвоения нет: наивное суммирование денежной секции ничего не испортит, потому
что первая строка нулевая. Но событие с нулевой суммой в append-only журнале —
шум, неотличимый потом от ошибки импорта, поэтому маркер отбрасывается.

**Займы бумаг** (5.4, 5.9) — это займ брокером у клиента: бумаги остаются в
собственности, позиция не меняется, выдача и возврат денег не двигают. В журнал
идёт только **вознаграждение за займ** — оно приходит в колонке `% по сделке`
строки «Возврат займа ценных бумаг» (2-я часть); у строки выдачи (1-я часть) эта
колонка пуста (A-21). Если допущение неверно, это проявится на бэкфилле
расхождением количества — сверка для того и нужна.

Место совершения: `Московская биржа (СПОТ: МБ T+)`,
`Московская биржа (МБ ФР: Т+: Корп. облигации)`, `OTC (Заем ОТС бумаги)`.

Полезные наблюдения:

* **дивиденд приходит как `Доход по финансовым инструментам`**, а что это именно
  дивиденд, сказано только в комментарии: «Выплата дивидендов, Акции
  обыкновенные ПАО "…", Рег. номер …, Дата регистрации …, Размер выплат на 1 ЦБ: …».
  Распознавать тип по колонке `Тип операции` недостаточно;
* **налог приходит отдельной строкой** — расщеплять gross/net не нужно, нужно
  связать `Налог` с его купоном или дивидендом. Связь восстанавливается только
  из комментария («Налог на доходы по дивидендам, 13%, Акции обыкновенные ПАО "…"»);
* **купон связан с бумагой только через комментарий**: «Погашение купона № RU000A105X64»
  — иногда ISIN, иногда краткое название («Погашение купона № Самолет БО-П13»,
  «№ ОФЗ 26219»). Значит разрешение инструмента по этому полю будет неточным и
  потребует справочника;
* комиссии в неторговых операциях не встретились: они сводятся в секции 1.1
  одной суммой за период по каждому виду. **Привязки комиссии к сделке нет в 8.1,
  но она есть прямо в строке сделки** (брокерская комиссия, комиссия ТС, гербовый
  сбор) — это лучше, чем предполагалось в `A-06`.

### Валюта — три разные колонки

В строке сделки **три** колонки валюты, и они не совпадают:

```
Цена одной ЦБ | Валюта цены | … | Сумма сделки | Валюта суммы сделки | Брокерская комиссия | Валюта брокерской комиссии
    0 000.0000 |     USD     | … |   0 000.00   |        RUR          |      0 000.00       |          RUR
```

Еврооблигация котируется в долларах, а рассчитывается в рублях. Одна колонка
«валюта» на всё означала бы, что долларовая сделка списывает доллары, — а
списывает она рубли. В архиве встречаются `RUR`, `USD`, `CNY`.

Отсюда же следует, что допущение A-08 (валюта по умолчанию) к секции сделок
неприменимо: валюта там всегда явная, вопрос только в том, какую из трёх брать.
Для денежного эффекта — `Валюта суммы сделки`.

## 5. Две даты

В строке сделки **четыре** даты: «Дата оплаты» плановая и фактическая, «Дата
поставки» плановая и фактическая. Плюс «Дата сделки». Это богаче модели из спеки
(`trade_date` и `settlement_date`):

* `trade_date` ← «Дата сделки»;
* `settlement_date` ← «Дата оплаты **фактическая**», а при её отсутствии
  (сделка ещё не рассчитана на дату отчёта) — плановая. Выбор нужно зафиксировать
  в `ASSUMPTIONS.md` (это уточнение к A-03: колонка есть, вопрос в том, какая из
  четырёх).

Остатки денежных средств печатаются в двух видах: «Исходящий остаток» и
«Исходящий **плановый** остаток (всего)». Для сверки по `settlement_date` нужен
первый; плановый включает незавершённые расчёты. Это тоже решение для реестра.

**С бумагами всё наоборот, и это не непоследовательность, а то же правило.**
Раздел 2 печатает «Количество ЦБ на конец периода» и «Плановое количество ЦБ»;
первое — бумаги, уже поставленные на счёт депо. Позиции считаются по
`trade_date`, поэтому контрольным служит **плановое** количество (A-26).
Контрольное число выбирается той же датой, какой считается проекция: деньги —
фактические по дате оплаты, бумаги — плановые по дате сделки. Иначе расхождение
возникает ровно на сделках рубежа периода, ради которых две даты и заведены.

## 6. Что это значит для парсера: `mapping_v2`

Разбор этого формата написан и работает — `adapters/broker/mapping_v2.py`.
Версия выбирается по форме документа в `adapters/broker/mapping.py`: наличие
нумерованных подписей разделов (`5.1`, `8.1.1`) означает выгрузку Excel. Имя
брокера в шапке признаком не служит — оно меняется при ребрендинге, нумерация
разделов не менялась за три года архива.

Что для этого понадобилось:

1. ~~**`tables.py` должен разворачивать `colspan`/`rowspan`** в сетку~~ —
   **сделано**: `RawTable` хранит ячейки с номерами колонок сетки, многострочная
   шапка склеивается в имя вида «Дата оплаты · Фактическая», а `as_dicts()`
   сопоставляет данные с шапкой по колонке, а не по порядковому номеру;
2. ~~**якорение секций по строкам, а не по таблицам**~~ — **сделано**:
   `adapters/broker/sections.py` режет плоскую сетку на секции и отдаёт каждую
   обычной `RawTable`, так что `resolve_columns` и `as_dicts` работают дальше без
   изменений. Подпись раздела опознаётся по номеру в первой ячейке строки; от
   суммы `1 234.56`, стоящей в строке одна, её отличает наличие буквы в хвосте;
3. ~~**двухстроечные шапки**~~ — **сделано**: продолжение шапки отличается от
   подзаголовка группы числом заполненных ячеек;
4. ~~**инструмент из строки-подзаголовка группы**~~ — **сделано**, но с
   оговоркой: ISIN в подзаголовке нет, он восстанавливается по разделу 2 (A-23);
5. ~~**связь налога и купона с инструментом из текста комментария**~~ —
   **сделано**: в комментарии ищется ISIN, затем известное наименование;
6. **восемь видов комиссий из секции 1** — не сделано и не нужно: это итоги за
   период, а не операции. Комиссии берутся из строк сделок по видам, включая
   гербовый сбор (`FeeKind.STAMP`, миграция `0002`);
7. `<style>` в `preceding_text` разбору v2 не мешает: якорение идёт по строкам
   таблицы, а не по тексту перед ней;
8. `x:num` не используется: `parse_decimal` разбирает видимый текст, и лишний
   источник правды означал бы два разных числа в одной ячейке.

Словарь колонок вынесен в отдельный `COLUMNS_V2` (`anchors.py`). Сводить его с
v1 нельзя: в v1 «дата поставки» — это дата расчётов, а здесь поставка бумаг и
оплата деньгами разнесены по разным парам колонок, каждая с плановой и
фактической датой.

### Разделы и что из них берётся

| Раздел | Что даёт журналу |
|---|---|
| `1`, `1.1` | контрольный остаток денег: строка «Исходящий остаток (всего)» (A-25) |
| `2` | контрольные остатки бумаг — по колонке «Плановое количество ЦБ» (A-26) — и справочник «наименование + регномер → ISIN» (A-23) |
| `4` | ничего: оценка активов — производные числа |
| `5.1`, `5.10` | сделки; пересечение разделов снимается ключом идемпотентности (A-22) |
| `5.4` | вознаграждение за заём (колонка «% по сделке» строки возврата) и брокерская комиссия строки (A-21) |
| `5.9` | ничего: расчётов ещё не было, деньги придут в 5.4 следующего отчёта (A-24) |
| `8.1`, `8.1.1` | купоны, дивиденды, налоги, ввод и вывод денег, погашение номинала |
| `8.2` | движение бумаг без денег: ввод, погашение, конвертация |
| прочие нумерованные | строки уходят в нераспознанные: незнакомый раздел — это потерянные операции |

Стадия 1 при этом осталась общей для обоих форматов, а `money.py`, `domain/`,
`jobs/` и CLI не изменились вовсе — ради этого стадии и разделены.

## 7. Что закрыто архивом, а что осталось

### Закрыто

| Вопрос шага 0 | Ответ по 14 отчётам |
|---|---|
| Есть ли номер сделки брокера | **да**, во всех: `B-000000-000000` в каждой секции сделок. Допущение A-01 подтверждено — ключ строится по номеру |
| Есть ли дата расчётов отдельно | **да**, и даже четыре: «Дата оплаты» и «Дата поставки», каждая плановая и фактическая |
| Комиссии одной суммой или по типам | **по типам**, прямо в строке сделки: брокерская, ТС, гербовый сбор; в секции 1 они же за период по восьми видам |
| Полный словарь `Тип операции` | собран, см. раздел 4 |
| Отличается ли валюта суммы от валюты цены | **да**, для еврооблигаций |
| Есть ли в архиве не-Excel отчёты | нет: все 14 — одна и та же выгрузка Excel |

### Закрыто прогоном по необезличенному архиву (15.09.2026)

Все вопросы ниже упирались в суммы. Прогон архива их снял: отчёты импортированы
целиком, сверка сходится после каждого месяца.

- [x] входит ли НКД в «Сумму сделки» (A-07) — **не входит**: разбор, прибавляющий
      НКД, сходится с исходящим остатком;
- [x] купон в `8.1.1` — **брутто**, налог отдельной строкой (A-04);
- [ ] как выглядит **отмена** сделки — в архиве не встретилась.

### Осталось: требует решения

- [ ] **`5.10 Исполнение обязательств по сделке`** содержит те же покупки и
      продажи, что и `5.1`. Дублирует она биржевую секцию или дополняет её
      внебиржевыми расчётами — решается сверкой: если учесть обе, денежный
      остаток разойдётся вдвое;
- [x] `% по сделке` в строке возврата займа — **сумма**, не ставка. Закрыто
      прогоном по архиву: разность «% по сделке» и брокерской комиссии по
      разделу 5.4 совпала с расхождением сверки до копейки (A-21);
- [ ] `Погашение номинала` — полное погашение или амортизация. Правило: полное,
      если в `8.2` есть парное списание всего остатка бумаги; проверяется тем,
      что позиция обязана уйти в ноль (A-20);
- [ ] как связывать строку `8.2` (бумаги) со строкой `8.1.1` (деньги) в одно
      событие: общих идентификаторов у них нет, только дата и бумага.
